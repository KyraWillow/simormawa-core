{
  "name": "backend",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "migrate": "bun --env-file=.env node-pg-migrate"
  },
  "dependencies": {
    "elysia": "latest"
  },
  "devDependencies": {
    "bun-types": "latest",
    "node-pg-migrate": "^9.0.0",
    "pg": "^8.23.0"
  },
  "module": "src/index.js"
}
